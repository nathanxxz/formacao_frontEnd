//add numero em vetor
let pergun=Number(prompt("informe quantas vezes quer add um numero no vetor"));
let numeros2=[]
for(let i=0;i<pergun;i++){
    const per=(prompt("informe um numero"))
    numeros2[i]=per
    console.log(numeros2[i])
}

